# Example Docker Compose file using secrets
# Copy this to docker-compose.override.yml and use with:
# docker compose --env-file .env up

# This file demonstrates using environment variables from .env file
# For production, consider using Docker secrets or a secrets management service

services:
  server:
    environment:
      JWT_SECRET: ${JWT_SECRET}
      # Other environment variables can be added here

